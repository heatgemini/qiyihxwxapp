<view class="container">
   
  <view class="con_cls" wx:if="{{id=='title'}}">
       <input type="text" data-id="{{id}}" value="{{val}}" auto-focus class="content" maxlength="64" placeholder="请输入信息" confirm-type="done" bindconfirm="save_info"/>   
  </view>
  <view class="con_cls" wx:if="{{id =='detail'}}">
       <textarea data-id="{{id}}" value="{{val}}" confirm-type="done" bindconfirm="save_info" auto-height class="content" placeholder="请输入信息" />
  </view>
</view>